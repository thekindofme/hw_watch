%div.category
  %p=link_to @category.name, category_path(@category)
  -#%img{:src => @category.image_url}
  %p description:    #{@category.description}
  %p=link_to "find products in this category", category_products_path(@category)
  %p=link_to "find shops selling products from this category", category_shops_path(@category)