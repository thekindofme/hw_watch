%div.shop
  %p=link_to @shop.name, shop_path(@shop)
  %p address:   #{@shop.address}
  %p rating:    #{@shop.rating}
  %p tel:       #{@shop.tel1}/#{@shop.tel2}
  %p www:       #{@shop.www_link}
  %p www shop:  #{@shop.shop_link}